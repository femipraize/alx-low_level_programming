#include "main.h"

/**
 * print_line - prints straight line n times.
 * @n: times straight line is printed.
 * Return: no return.
 */
void print_line(int n)
{
	int i = 0
		while (i < n && n > 0)
		{
			_putchar('-');
			i++;
		}
	_putchar('\n');
}
